"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
function arrayToHash(a) {
    let hash = {};
    for (let item of a) {
        hash[item] = true;
    }
    return hash;
}
exports.arrayToHash = arrayToHash;
function filterHash(hash, keyObj) {
    let res = {};
    for (let key in hash) {
        if (keyObj[key]) {
            res[key] = hash[key];
        }
    }
    return res;
}
exports.filterHash = filterHash;
function mapHashToArray(hash, func) {
    let res = [];
    for (let key in hash) {
        res.push(func(hash[key], key));
    }
    return res;
}
exports.mapHashToArray = mapHashToArray;
